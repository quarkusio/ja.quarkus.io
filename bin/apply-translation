#!/usr/bin/env bash
set -eu

#
# This script emits translated source directory into the `translated` directory
#

SCRIPT_DIR=$(cd $(dirname $0); pwd)
L10N_HOME=$(cd $(dirname $SCRIPT_DIR); pwd)
cd $L10N_HOME

./bin/exec-po4a-translate
./bin/exec-override
