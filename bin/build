#!/usr/bin/env bash
set -eu

#
# This script builds the localized site under the `docs` directory
#

SCRIPT_DIR=$(cd $(dirname $0); pwd)
L10N_HOME=$(cd $(dirname $SCRIPT_DIR); pwd)
cd $L10N_HOME

./bin/apply-translation
./bin/exec-jekyll
